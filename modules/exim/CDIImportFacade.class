<?php

class CDIImportFacade extends CDIAbstractDataTypeFacade {
  
  public function import(CDIExportChunk $chunk) {
    $command = new CDIImportCommand($chunk);
    $this->fireCommand($command);
    return $command->getDataObject();
  }
  
}