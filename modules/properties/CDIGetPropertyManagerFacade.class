<?php

class CDIGetPropertyManagerFacade extends CDIAbstractDataTypeFacade {
  
  public function getPropertyManager($propertyKey) {
    $command = new CDIGetPropertyManagerCommand($propertyKey);
    $this->fireCommand($command);
    return $command->getManager();
  }
  
}