<?php

class CDIListFacade extends CDIAbstractDataTypeFacade {
  
  public function listItems($size = NULL, $page = NULL) {
    $command = new CDIListCommand($size, $page);
    $this->fireCommand($command);
    return $command->getList();
  }
  
}
