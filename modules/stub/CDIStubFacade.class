<?php

class CDIStubFacade extends CDIAbstractDataTypeFacade {
  
  public function stub($defaults = array()) {
    $command = new CDIStubCommand($defaults);
    $this->fireCommand($command);
    return $command->getDataObject();
  }
  
}